# Runtime dependencies (direct, version-pinned).
# Re-generate lock file: pip-compile requirements.in -o requirements.txt
fastapi==0.141.1
uvicorn[standard]==0.52.4
# Keep the Linux event-loop extra explicit while allowing the same lock on Windows.
uvloop==0.22.1; sys_platform != "win32"
python-multipart==0.0.32
typing-extensions==4.16.0
openpyxl==3.1.5
